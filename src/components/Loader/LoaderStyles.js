import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  centerLoader: {
    flex:           1,
    alignItems:     'center',
    justifyContent: 'center',
    padding:        8
  }
})
